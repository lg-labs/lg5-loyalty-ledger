---
kind: spec
name: verify-report
feature: 004-project-docs
version: 0.1.0
description: Verification report cross-checking every PRD acceptance criterion for feature 004-project-docs (loyalty-ledger documentation surface) against test evidence. Gate decision is VERIFIED WITH OVERRIDE because TASK-012 (live-URL post-merge probe) is sanctioned-deferred-to-post-merge by the spec itself.
---

# Verify Report — `004-project-docs`

> **Gate decision (preview): 🟡 VERIFIED WITH OVERRIDE.** All pre-merge
> requirements pass with concrete CI + file evidence. TASK-012 (live
> URL probes for REQ-001..006, REQ-013..014) is **structurally
> impossible pre-merge** because the live deploy jobs are gated by
> `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`
> ([`design.md` §7.9 rows 3-5](design.md)); the spec acknowledges this
> in TASK-012's "Given … feature branch has been merged to `main`"
> precondition. The override is documented in §6.

## 0. Run metadata

| Field | Value |
|-------|-------|
| Generated by | `sdd-verifier` (overridden per orchestrator instructions — Java/Maven steps replaced with docs-build steps; see §5 prelude) |
| Run timestamp (UTC) | `2026-05-11T18:26:49Z` |
| Git SHA | `5d56076` |
| Branch | `feature/004-project-docs` |
| Build command (canonical) | n/a — no JVM module in this feature (RULE-004 deviation documented in [`plan.md`](plan.md)) |
| Build command (replacement, RULE-017 adapted) | `make docs-build-firebase` |
| Acceptance command (canonical) | n/a — no JVM test runtime ([`design.md` §9](design.md)) |
| Evidence-of-record (replacement) | PR #6 CI runs `25684361623` (baseline, all green), `25686294164` (TASK-011 synthetic mutation, all green w/ warnings), `25687611259` (TASK-011 revert, all green, clean) |
| Build result | ✅ pass (`make docs-build-firebase` exit 0, four `::warning::` annotations from `check-artifacts.mjs` for absent local upstream artifacts — expected outside CI) |
| Acceptance result | ✅ pass (CI runs above; ATDD N/A) |

## Adaptation note (orchestrator-sanctioned)

Per the orchestrator brief and consistent with [`plan.md` "Deviation
from RULE-004"](plan.md) and [`design.md` §9](design.md), this feature
introduces **zero Java/Spring code**. The canonical sdd-verifier step 5
(`make all-build` + `make run-acceptance-test`) is **N/A**. Replacement
evidence is:

1. Local build: `make docs-build-firebase` (executed in this run — exit 0).
2. Local artifact presence: six HTML pages emitted under
   `docs/site/.vitepress/dist/{,architecture,api,events,adr,runbook}/index.html`.
3. CI evidence: GitHub Actions runs on PR #6 (16 jobs each).
4. File evidence in the repo at known paths.

Constitutional rules tied to Spring/Maven (RULE-001..002, RULE-006,
RULE-007, RULE-008, RULE-010..015) are **N/A** for this feature. See §5.

## 1. AC ↔ evidence matrix

> Evidence types used here (in order of strength):
> 1. CI run + job id + annotation/artifact (strongest).
> 2. File in repo at expected path.
> 3. Live URL probe (post-merge only — marked 🟡 manual, see §4).

| REQ | AC summary | Covered by | Evidence | Status |
|-----|------------|------------|----------|:-----:|
| REQ-001 | Visitor opening well-known location lands on home identifying loyalty-ledger | TASK-012 (live probe) | Pre-merge: `docs/site/index.md` exists; CI run `25684361623` job `75408565535` (Docs Build Firebase, conclusion=success) emits `docs/site/.vitepress/dist/index.html`. Live probe deferred. | 🟡 |
| REQ-002 | Home → one-click link to architecture overview | TASK-002 | `docs/site/.vitepress/config.ts:1` `nav` entry for Architecture; `docs/site/architecture/index.md` exists; built HTML at `docs/site/.vitepress/dist/architecture/index.html` (local build). Live probe deferred. | 🟡 |
| REQ-003 | Home → one-click link to sync (API) contract | TASK-002 | `docs/site/.vitepress/config.ts` nav entry "API"; `docs/site/api/index.md`; dist HTML `docs/site/.vitepress/dist/api/index.html`. Live probe deferred. | 🟡 |
| REQ-004 | Home → one-click link to async (events) contract | TASK-002 | `docs/site/.vitepress/config.ts` nav entry "Events"; `docs/site/events/index.md`; dist HTML `docs/site/.vitepress/dist/events/index.html`. Live probe deferred. | 🟡 |
| REQ-005 | Home → one-click link to latest acceptance-test (Allure) report | TASK-002, TASK-008 | Nav entry to `https://lglabs-loyalty-allure.web.app/` in `docs/site/.vitepress/config.ts`; CI job `firebase-deploy-allure` declared in `.github/workflows/c-integration.yml` (verified by `gh run view` job listing — currently `skipped` on PR runs as designed). Live probe deferred. | 🟡 |
| REQ-006 | Home → one-click link to ADR index | TASK-002 | Nav entry "ADRs"; `docs/site/adr/index.md`; dist HTML `docs/site/.vitepress/dist/adr/index.html`. Live probe deferred. | 🟡 |
| REQ-007 | Home → onboarding runbook with ≥3 stated sections (setup checklist, repo tour, cross-links) | TASK-010 | File `docs/site/runbook/index.md` exists; `grep -cE '^##' docs/site/runbook/index.md` → 8 (≥3); dist HTML `docs/site/.vitepress/dist/runbook/index.html`. | ✅ |
| REQ-008 | Surface reachable without authentication | TASK-005, TASK-008 | `docs/site/firebase.json` declares public hosting (no auth headers); Pages workflow `pages-deploy` job uses `actions/deploy-pages@v4` (public by default); pre-merge: no auth code added anywhere. Live probe deferred. | 🟡 |
| REQ-009 | Surface regenerates on every trunk advance, no human action | TASK-007, TASK-008 | `.github/workflows/c-integration.yml` `Docs Build (Firebase)` + `Pages Deploy (docs)` + `Firebase Deploy (docs)` are triggered on `push` to `main`. Run `25684361623` proves the build chain green; deploy jobs `pages-deploy`, `firebase-deploy-docs` correctly `skipped` on PR (`if: github.event_name == 'push' && github.ref == 'refs/heads/main'`). Live probe deferred. | 🟡 |
| REQ-010 | End-to-end lag < 10 min trunk-advance → reflected | TASK-008, TASK-012 | Build job `Docs Build (Firebase)` on run `25684361623` completed in ~1m17s (well under 10m); deploys add seconds. Live probe to confirm timestamp freshness post-merge is TASK-012. | 🟡 |
| REQ-011 | Surface reflects current trunk only (no per-release history) | TASK-008 | `docs/site/firebase.json` uses single hosting target (no versioned paths); `cleanUrls: true`; `pages-deploy` overwrites prior deployment by design. Live probe deferred. | 🟡 |
| REQ-012 | `docs/preview` label opt-in produces shareable preview | TASK-009 | `.github/workflows/c-integration.yml` job `Firebase Preview (PR channel)` declared with `if: ... contains(github.event.pull_request.labels.*.name, 'docs/preview') ...`; run `25686294164` shows job conclusion=`skipped` (no label on PR #6 at run time). Pre-merge label round-trip is TASK-012. | 🟡 |
| REQ-013 | Preview URL distinct, scoped, sharable without auth | TASK-009 | Workflow uses `firebase hosting:channel:deploy pr-${PR_NUMBER} --only docs --expires 7d` which produces `https://lglabs-loyalty-docs--pr-<N>-<hash>.web.app/`. Live probe deferred. | 🟡 |
| REQ-014 | Preview expires after 7 days (no manual cleanup) | TASK-009 | `--expires 7d` flag in `firebase hosting:channel:deploy` command in workflow. Calendar-deferred sub-check explicitly authorized by TASK-012 AC ("may be deferred to a calendar follow-up note"). | 🟡 |
| REQ-015 | Without `docs/preview` label, no preview produced | TASK-009 | `if:` guard in workflow; observed on runs `25684361623`, `25686294164`, `25687611259`: `Firebase Preview (PR channel)` job conclusion=`skipped` (PR has no `docs/preview` label). | ✅ |
| REQ-016 | Surface is open-ended (no contract change to add sections) | TASK-002 | `docs/site/.vitepress/config.ts` `sidebar` is a static array; adding a section is a markdown-only change. Verified by repo structure — no schema gate. | ✅ |
| REQ-017 | Local (client-side) search across all linked docs | TASK-002 | `docs/site/.vitepress/config.ts:32` `search: { provider: 'local' }`. Local build emits search index in `dist/assets/`. Live probe deferred. | 🟡 |
| REQ-018 | Broken internal link → build succeeds + warning emitted | TASK-004, TASK-007, TASK-011 | CI run `25686294164` job `75414816783` (Docs Build Firebase) annotation `path=architecture, message="Broken link architecture/does-not-exist -> 404", annotation_level=warning`, **job conclusion=success** (REQ-018 fully proven). | ✅ |
| REQ-019 | Missing source artifact → previous-version-with-stale-indicator or "no content yet" placeholder | TASK-003, TASK-007, TASK-011 | CI run `25686294164` job `75414816783` annotation `path=public/dependency-graph.png, message="The dependency graph was not produced in the most recent CI run.", annotation_level=warning`, **job conclusion=success**; dist artifact `docs-dist-firebase` from same run contains the architecture section rendering the placeholder copy (per `docs/site/scripts/check-artifacts.mjs` + `_placeholder.md` writer). | ✅ |
| REQ-020 | Each surface shows visible source-state indicator (sha + timestamp; previews add PR#) | TASK-002 | `docs/site/.vitepress/theme/SourceStateFooter.vue` lines 2-8 consume `__COMMIT_SHA__`, `__BUILD_TIME__`, `__PR_NUMBER__`; registered on `layout-bottom` slot at `docs/site/.vitepress/theme/index.ts:10`; `__PR_NUMBER__` injected from env in `docs/site/.vitepress/config.ts:36-38`. Live visual probe deferred. | 🟡 |

## 2. Coverage summary

| Bucket | Count | % |
|--------|------:|--:|
| ✅ pass | 6 | 30% |
| ⚠ flaky | 0 | 0% |
| ❌ fail | 0 | 0% |
| ⚪ uncovered | 0 | 0% |
| 🟡 manual (post-merge live probe via TASK-012) | 14 | 70% |
| **Total AC** | **20** | **100%** |

The 14 🟡 rows do not indicate missing coverage — they indicate
evidence that **structurally cannot** be gathered on a feature branch
(live-URL HTTP probes against domains served by main-only deploy
jobs). All 14 have concrete pre-merge file/CI evidence + an explicit
post-merge probe in TASK-012 AC.

## 3. TASK ↔ REQ traceability

| REQ | Implemented by TASK(s) | Test/validation TASK(s) | Status |
|-----|------------------------|--------------------------|:-----:|
| REQ-001 | TASK-001, TASK-002, TASK-005, TASK-006, TASK-007, TASK-008 | TASK-012 | 🟡 |
| REQ-002 | TASK-002 | TASK-012 | 🟡 |
| REQ-003 | TASK-002 | TASK-012 | 🟡 |
| REQ-004 | TASK-002 | TASK-012 | 🟡 |
| REQ-005 | TASK-002, TASK-005, TASK-008 | TASK-012 | 🟡 |
| REQ-006 | TASK-002 | TASK-012 | 🟡 |
| REQ-007 | TASK-002, TASK-010 | TASK-012 | ✅ |
| REQ-008 | TASK-005, TASK-008 | TASK-012 | 🟡 |
| REQ-009 | TASK-007, TASK-008 | TASK-012 | 🟡 |
| REQ-010 | TASK-007, TASK-008 | TASK-012 | 🟡 |
| REQ-011 | TASK-005, TASK-008 | TASK-012 | 🟡 |
| REQ-012 | TASK-005, TASK-009 | TASK-012 | 🟡 |
| REQ-013 | TASK-005, TASK-009 | TASK-012 | 🟡 |
| REQ-014 | TASK-009 | TASK-012 | 🟡 |
| REQ-015 | TASK-009 | observed on PR #6 runs | ✅ |
| REQ-016 | TASK-002 | n/a | ✅ |
| REQ-017 | TASK-002 | TASK-012 | 🟡 |
| REQ-018 | TASK-004, TASK-007 | TASK-011 | ✅ |
| REQ-019 | TASK-003, TASK-007 | TASK-011 | ✅ |
| REQ-020 | TASK-002, TASK-009 | TASK-012 | 🟡 |

All 20 REQs have at least one implementing TASK. Every TASK referenced
above is `Status: done` except TASK-012 (`Status: todo` — see §4).

## 4. Manual verifications (post-merge — sanctioned by spec)

> TASK-012 is the canonical post-merge verification TASK. Its
> precondition is explicit: "**Given** … the feature branch has been
> merged to `main`". The live deploy jobs (`pages-deploy`,
> `firebase-deploy-docs`, `firebase-deploy-allure`) are gated by
> `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`
> per [`design.md` §7.9 rows 3-5](design.md). On a feature branch they
> are correctly `skipped`. This procedural deferral is intrinsic to the
> chosen design (ADR-002 dual deploy + ADR-003 separate Allure site)
> and is therefore sanctioned by the spec itself.

| REQ | AC | Procedure | Verified by | Date | Status |
|-----|----|-----------|-------------|------|:-----:|
| REQ-001 | Home identifies loyalty-ledger | TASK-012 step (1)+(2): open `https://lglabs-pentagon.github.io/lg5-loyalty-ledger/` and `https://lglabs-loyalty-docs.web.app/` in incognito | implementer (post-merge) | TBD | 🟡 |
| REQ-002 | Architecture link works | TASK-012 step (1)+(2): click each nav entry | implementer (post-merge) | TBD | 🟡 |
| REQ-003 | Sync (API) contract link works | TASK-012 step (1)+(2): click "API" nav entry | implementer (post-merge) | TBD | 🟡 |
| REQ-004 | Async (Events) contract link works | TASK-012 step (1)+(2): click "Events" nav entry | implementer (post-merge) | TBD | 🟡 |
| REQ-005 | Latest Allure report served | TASK-012 step (3): open `https://lglabs-loyalty-allure.web.app/` in incognito | implementer (post-merge) | TBD | 🟡 |
| REQ-006 | ADR index link works | TASK-012 step (1)+(2): click "ADRs" nav entry | implementer (post-merge) | TBD | 🟡 |
| REQ-008 | All entries reachable without auth | TASK-012 steps (1)-(3) in incognito (no cookies/tokens) | implementer (post-merge) | TBD | 🟡 |
| REQ-009 | Surface regenerates on trunk advance | TASK-012: observe deploy jobs run on the merge-to-main commit | implementer (post-merge) | TBD | 🟡 |
| REQ-010 | < 10 min lag | TASK-012 step (c): compare footer `__BUILD_TIME__` to merge time | implementer (post-merge) | TBD | 🟡 |
| REQ-011 | No per-release history | TASK-012: re-visit URLs after a 2nd main advance | implementer (post-merge) | TBD | 🟡 |
| REQ-012 | `docs/preview` label round-trip | TASK-012 step (4): open throwaway PR, apply label, observe preview URL bot-comment | implementer (post-merge) | TBD | 🟡 |
| REQ-013 | Preview URL incognito-reachable | TASK-012 step (4): open URL in incognito | implementer (post-merge) | TBD | 🟡 |
| REQ-014 | Preview expires after 7 days | TASK-012 step (e): calendar follow-up at T+7d+1h (explicitly authorized to defer beyond TASK-012 closure) | implementer (post-merge) | TBD | 🟡 |
| REQ-017 | Local search ≥1 hit | TASK-012 step (a): query "loyalty-ledger" in the search box | implementer (post-merge) | TBD | 🟡 |
| REQ-020 | Source-state footer on every page (main + preview) | TASK-012 step (c)+(d): visually confirm footer text on each surface | implementer (post-merge) | TBD | 🟡 |

## 5. Constitutional check

> Rules tied to Spring/Maven/Kafka/Avro are N/A because this feature
> introduces no Java code (deviation from RULE-004 documented in
> [`plan.md`](plan.md), accepted by stakeholder).

| RULE | Touched by feature? | Verdict | Evidence |
|------|:-------------------:|:------:|----------|
| RULE-001 (stack baseline: Spring Boot 3.4.2 / JDK 21) | no | n/a — no Java/Spring code in feature 004 | feature touches only `docs/site/`, `.github/workflows/`, `Makefile`, `docs/specs/004-project-docs/` |
| RULE-002 (parent POM version) | no | n/a — no Maven module added; existing `pom.xml` parent unchanged | `git diff main -- pom.xml` empty |
| RULE-004 (Maven module shape) | no — explicit deviation | n/a (deviation accepted) | [`plan.md` §"Deviation from RULE-004"](plan.md) |
| RULE-006 (`vnd.api.v1+json` on controllers) | no | n/a — no Spring controller in feature 004 | — |
| RULE-007 (Avro `.avsc` for Kafka payloads) | no | n/a — no Kafka producer/consumer in feature 004 | — |
| RULE-008 (Outbox `@Version` + `OutboxStatus`) | no | n/a — no JPA entity in feature 004 | — |
| RULE-010 (Kafka listener NO-OP on `OptimisticLockingFailureException`) | no | n/a — no `@KafkaListener` in feature 004 | — |
| RULE-011 (Outbox scheduler `@ConditionalOnProperty(scheduling.enabled)`) | no | n/a — no `@Scheduled` bean in feature 004 | — |
| RULE-012 / RULE-013 (ATDD profiles `test,local` + base classes) | no | n/a — no JVM test runtime ([`design.md` §9](design.md)) | — |
| RULE-014 (config prefixes `kafka-config.*`, `<svc>-service.*`, …) | no | n/a — no Spring config added in feature 004 | — |
| RULE-015 (DDD building blocks from `ddd-common-domain`) | no | n/a — no domain code in feature 004 | — |
| RULE-017 (Make targets are the canonical entrypoint; always build before verify) | **yes** | ✅ | Six `docs-*` targets added to `Makefile` (lines 118-137); CI invokes them (`.github/workflows/c-integration.yml` jobs `Docs Build (Pages)` / `Docs Build (Firebase)`); verifier executed `make docs-build-firebase` in this run (exit 0). |
| RULE-018 (canonical CI patterns) | **yes** | ✅ | CI workflow `c-integration.yml` reuses the existing pnpm+node setup pattern; `defaults: { run: { working-directory: docs/site } }` consistently applied across all 6 added docs jobs; conclusions observed: build jobs `success`, gated deploy jobs `skipped` on PR (correct). |
| Repo-wide rule (no unauthorized changes to `lg5-spring-parent` version) | n/a (not touched) | ✅ | `git diff main -- '*pom.xml'` shows no diff. |

- ✅ rules verified: 2 (RULE-017, RULE-018)
- ✅ rules confirmed-unchanged: 1 (parent POM)
- n/a rules: 11 (all Spring/Maven/Kafka rules — feature is docs-only)
- ⚠ rules requiring manual review: 0
- ❌ rules violated: 0

## 6. Gaps and overrides

| Gap | Severity | Decision | Owner | Tracking |
|-----|----------|----------|-------|----------|
| TASK-012 `Status: todo` (live-URL probes pending) | structural (procedural) | **Sanctioned deferral** — TASK-012 AC explicitly requires "feature branch has been merged to `main`"; live deploy jobs are gated by `if: github.event_name == 'push' && github.ref == 'refs/heads/main'` ([`design.md` §7.9](design.md)); deferral is intrinsic to the chosen design (ADR-002 + ADR-003). No tech-debt ADR needed because no AC is being skipped — only its observability is time-shifted to post-merge. | stakeholder (`lglabs`) | TASK-012 row in [`tasks.md`](tasks.md); orchestrator post-merge follow-up |
| 14 AC marked 🟡 manual | advisory | **Accepted** — all 14 have pre-merge file + CI evidence; the manual step is the post-merge live-URL probe in TASK-012. Procedure documented in §4. | implementer | TASK-012 |
| REQ-014 calendar sub-check (preview expires at T+7d) | advisory | **Accepted** — explicitly authorized to defer beyond TASK-012 closure by TASK-012 AC ("may be deferred to a calendar follow-up note rather than blocking TASK closure"). | implementer | calendar follow-up |
| Pre-existing CI annotation: "Node.js 20 actions are deprecated" on `docs-*` jobs | advisory | **Out of scope** for this verify — flagged by GitHub Actions runner, not by feature 004 code. Affects shared actions across the whole workflow. | infra team | not blocking |
| Pre-existing CI annotation: "Broken link `https://lglabs-loyalty-allure.web.app/` → 404" on every docs page | advisory | **Expected pre-merge** — the Allure Firebase site is only deployed post-merge by `firebase-deploy-allure` (main-only). Linkinator wrapper correctly emits this as a `::warning::` (REQ-018 behavior) and the build remains green. Will self-resolve on first main push. | implementer | self-resolves post-merge |

## 7. Gate decision

- [ ] ✅ **VERIFIED** — all AC ✅, all RULE ✅.
- [x] 🟡 **VERIFIED WITH OVERRIDE** — 14 AC are 🟡 pending post-merge
      live-URL probe via TASK-012. The deferral is **sanctioned by the
      spec itself** (TASK-012 AC requires "feature branch merged to
      main"; deploy jobs gated to `push && main`). Pre-merge evidence
      (CI runs + repo files) covers every REQ. No tech-debt ADR is
      required because no AC is being abandoned or weakened — only its
      live-URL observability is time-shifted to T+0 post-merge. ADR-006
      (a scope-deferral ADR for the future upstream skill) is unrelated
      to AC coverage and is not invoked as the override basis.
- [ ] ❌ **NOT VERIFIED**.

**Override basis**: structural — spec design (ADR-002 + ADR-003) and
GitHub Pages / Firebase deploy gating make pre-merge live probing
impossible by construction; the spec explicitly chose this design and
the verifier-deferred TASK-012 to compensate.

## 8. Suggested next step

1. Merge PR #6 to `main` (24 commits, all gates green pre-merge).
2. Immediately after merge, execute TASK-012 acceptance procedure
   verbatim (4 steps, ~15 min wall clock). Flip TASK-012 `Status: todo`
   → `Status: done` once the four observable checks pass.
3. Re-run `/sdd-verify 004-project-docs` after step 2 to produce a
   **second** verify-report.md (this is point-in-time per the
   sdd-verifier hard rules; a new run produces a new report).
4. Mark feature `verified` once the post-merge report reaches ✅.

## Definition of Done (Verify report)

- [x] §0 run metadata fully populated (timestamps, SHA, results).
- [x] §1 matrix has one row per REQ × AC pair from the PRD (20 rows for 20 REQs; no REQ has multi-scenario AC in this PRD).
- [x] Every non-✅ row in §1, §3, or §5 appears in §6 with a decision.
- [x] §7 gate decision is explicitly ticked (🟡 VERIFIED WITH OVERRIDE).
- [x] If gate is 🟡: override basis is documented in §6 + §7.
- [x] Report committed at `docs/specs/004-project-docs/verify-report.md`.
