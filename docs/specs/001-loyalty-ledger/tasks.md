---
kind: tasks
feature: 001-loyalty-ledger
version: 0.1.0
description: 20 atomic, verifiable TASK-NNN derived from plan.md. Consumed one-at-a-time by /sdd-implement.
---

# Tasks — `loyalty-ledger`

> Generated by `/sdd-tasks` from [`plan.md`](plan.md), backed by
> [`prd.md`](prd.md) and the 5 ADRs under [`adr/`](adr/).
> Each task is atomic (≤1 day, 1-3 commits), references the source
> REQ-NNN / RULE-NNN / ADR-NNN, and carries Given/When/Then acceptance
> criteria. `/sdd-implement <task-id>` consumes one task at a time and
> updates its `Status` field upon successful commit.

## Dependency graph

```
                                ┌── TASK-005 (movement JPA) ──┐
TASK-001  ──►  TASK-002  ──►  TASK-004 ──┤                              ├──┐
(scaffold)    (parent +       (Liquibase│── TASK-006 (balance JPA)  ──┤  │
              build green)    DDL)      │                              │  │
                                        └── TASK-007 (processed +     ─┤  │
                                            outbox JPA)                │  │
                                                                       ▼  ▼
                          ┌────── TASK-003 (domain-core POJOs) ──────┐ │
                          │                                          │ │
                          ▼                                          ▼ ▼
              ┌── TASK-008 (Avro→domain mapper) ──┐    TASK-011 ───────┤
              │                                   ▼    (handler +      │
              ├── TASK-009 (OrderPaid listener)  ─┤     dedup gate)    │
              ├── TASK-010 (OrderCancelled L.)   ─┤                    │
              └── TASK-010b (OrderRefunded L.)   ─┘                    │
                                                                        │
                                              ┌── TASK-012 (payload→Avro mapper) ─┐
                                              ├── TASK-013 (outbox scheduler+pub) ─┤
                                              └── TASK-014 (schema registration)  ─┤
                                                                                    │
                                              ┌── TASK-015 (REST balance) ────────┤
                                              ├── TASK-016 (REST movements paged) ─┤
                                              └── TASK-017 (error handling/DTO)   ─┤
                                                                                    │
                                              ┌── TASK-018 (ATDD infra: TC+WM)   ──┤
                                              ├── TASK-019 (ATDD scenarios)       ─┤
                                              └── TASK-020 (final code review)    ◄┘
```

DAG verified — no cycles. M0 (1-2) → M1 (3-7) → M2 (8-11) → M3 (12-14)
→ M4 (15-17) → M5 (18-20). TASK-003 (`domain-core`) is a sibling of
the persistence work (M1) and a dependency of the application-service
work (M2-M3).

---

## TASK-001 — Scaffold the 8-module service from `blank-service`

- **Status:** done
- **References:** RULE-002, RULE-004, RULE-005, RULE-014, ADR-001 (no saga modules)
- **Depends on:** —
- **Modules touched:** all 8 (creation)
- **Skill:** `lg5-new-service`
- **Command / Subagent:** `/scaffold-service lg5-loyalty-ledger com.lg.platform.loyalty`
- **Acceptance:**
  - **Given** an empty repo (only `AGENTS.md`, `README.md`, `.agent-os/`, `docs/specs/001-loyalty-ledger/`)
  - **When** the scaffold command finishes
  - **Then** the working tree contains the 8 modules from RULE-004 (`-domain/{-domain-core,-application-service}`, `-api`, `-data-access`, `-message/{-message-core,-message-model}`, `-container`, `-acceptance-test`, `-support`), each with a `pom.xml`, no module under `src/main/java/**` references custom framework annotations (RULE-005), and all module `pom.xml` files declare the parent `com.lg5.spring:lg5-spring-parent:1.0.0-alpha.cbb6783` (RULE-002).

> Completed in commit <sha-placeholder>; ATDD modules empty, skeleton compiles green.

## TASK-002 — Verify build + wire `application.yaml` test/local profiles

- **Status:** done
- **References:** RULE-001, RULE-002, RULE-012, RULE-014, RULE-017
- **Depends on:** TASK-001
- **Modules touched:** `lg5-loyalty-ledger-container`
- **Skill:** `lg5-spring-overview`
- **Command / Subagent:** (none — manual + `make install-skip-test`)
- **Acceptance:**
  - **Given** the scaffolded modules from TASK-001
  - **When** `make install-skip-test` is run
  - **Then** the build returns `0` and produces 8 JARs; `application.yaml` declares profiles `test` and `local`, sets `scheduling.enabled: false` for `test`, and uses canonical config prefixes `kafka-config.*`, `loyalty-ledger-service.*`, `spring.datasource.*` (RULE-014); the JDK in use is 21 and Spring Boot is `3.4.2` (RULE-001).

> Completed in commit <sha-placeholder>; build green with JDK 21 pinned via .tool-versions; profiles verified. Deferred: lg5-spring-parent@cbb6783 transitively resolves Spring Boot 3.3.5 (RULE-001 expects 3.4.2) — framework-side concern, requires a newer parent SHA; out of scope for this TASK.

## TASK-003 — Define `domain-core`: `Movement`, `CustomerBalance`, `ProcessedInputEvent`, `CustomerBalanceUpdatedEvent`, value objects

- **Status:** done
- **References:** REQ-002, REQ-007, REQ-008, REQ-013, REQ-014, RULE-003, RULE-005, RULE-008, RULE-016
- **Depends on:** TASK-001
- **Modules touched:** `lg5-loyalty-ledger-domain-core`
- **Skill:** `lg5-spring-overview`
- **Command / Subagent:** (none — direct authoring)
- **Acceptance:**
  - **Given** unit tests covering `Movement.ofCredit`/`ofDebit` invariants (sign matches cause; `delta != 0`), `CustomerBalance.applyDelta` allowing negative results, `CustomerBalance.empty(id)` factory, and `ProcessedInputEvent` factories per outcome
  - **When** `mvn test -pl lg5-loyalty-ledger-domain-core` runs
  - **Then** all unit tests pass; `domain-core/pom.xml` declares **no** Spring dependency (RULE-003 verified by `mvn dependency:tree`); aggregates extend `AggregateRoot` from `lg5-common-domain` (RULE-016); every aggregate exposes a `version: int` field (RULE-008); `Movement` has no public mutator method (REQ-013).

> Completed in commit <sha-placeholder>; 19 test methods green; RULE-003/005/008/016 audited.
> Amended via fix(TASK-003) at 741540d to remove domain-side version bump in `CustomerBalance.applyDelta` — JPA `@Version` (added in TASK-006) is the single source of truth for the optimistic-locking token; matches food-ordering-system convention and resolves a dual-ownership conflict surfaced during TASK-006 implementation.

## TASK-004 — Liquibase DDL: `loyalty` schema + 4 tables + Postgres ENUMs + indexes

- **Status:** done
- **References:** REQ-003, REQ-006, REQ-007, REQ-009, REQ-010, REQ-013, REQ-014, REQ-015, RULE-008, ADR-003, ADR-004
- **Depends on:** TASK-002
- **Modules touched:** `lg5-loyalty-ledger-data-access`
- **Skill:** `lg5-outbox`
- **Command / Subagent:** (none — direct SQL authoring)
- **Acceptance:**
  - **Given** `data-model.md` §JPA tables as the source of truth
  - **When** the Liquibase changelog `db/changelog/db.changelog-master.yaml` (with `ddl-loyalty-v.0.0.1.yaml` included) is applied to a Testcontainers Postgres in an integration test
  - **Then** the `loyalty` schema exists; tables `movement`, `customer_balance`, `processed_input_event`, `outbox` exist with the columns, types, defaults, and indexes specified in `data-model.md` (including `idx_movement_customer_appended` on `(customer_id, appended_at DESC, id DESC)` for REQ-010, the **unique constraint `uq_processed_event_type_id` on `(originating_event_type, originating_event_id)`** for REQ-003/REQ-006, the `loyalty_cause` ENUM (`ORDER_PAID, ORDER_CANCELLED, ORDER_REFUNDED`), the `processed_input_outcome` ENUM, and the `outbox_status` ENUM); a no-op `down` is documented in the file header.

> Completed in commit <sha-placeholder>; Liquibase changelog applied by `LiquibaseMigrationIT` (11 assertions) on a Testcontainers Postgres in CI; deviation: framework ships Liquibase (not Flyway) — TASK-004 wording corrected accordingly.

## TASK-005 — JPA entity + repository for `movement`

- **Status:** done
- **References:** REQ-013, REQ-014, RULE-008, ADR-004
- **Depends on:** TASK-003, TASK-004
- **Modules touched:** `lg5-loyalty-ledger-data-access`
- **Skill:** `lg5-outbox`
- **Command / Subagent:** (none)
- **Acceptance:**
  - **Given** the `movement` table created by TASK-004 and the `Movement` aggregate from TASK-003
  - **When** a `@DataJpaTest` (or `Lg5TestBootPortNone` IT, RULE-012) inserts a `MovementJpaEntity` and reads it back
  - **Then** the entity is persisted with `@Enumerated(EnumType.STRING)` for `cause`, the JPA-side `version` field is present (RULE-008), the repository exposes only **read** methods + a `save(...)` that performs `INSERT` (no `update`/`delete` operations declared) honoring REQ-013, and a domain-mapper round-trip (`Movement` → `MovementJpaEntity` → `Movement`) preserves every field including `originatingEventId` (REQ-014).

> Completed in commit <sha-placeholder>; `MovementJpaRepository` extends only `org.springframework.data.repository.Repository` (no delete/update generated), `MovementLedgerRepositoryIT` asserts round-trip + REQ-013 surface + REQ-004 `existsCreditFor` (5 tests).

## TASK-006 — JPA entity + repository for `customer_balance` (projection upsert)

- **Status:** done
- **References:** REQ-007, REQ-008, REQ-009, RULE-008, RULE-016, ADR-004
- **Depends on:** TASK-003, TASK-004
- **Modules touched:** `lg5-loyalty-ledger-data-access`
- **Skill:** `lg5-outbox`
- **Command / Subagent:** (none)
- **Acceptance:**
  - **Given** the `customer_balance` table from TASK-004
  - **When** an IT applies a sequence of deltas to one customer (e.g. `+100, -150, +50`)
  - **Then** the resulting row reflects `balance = 0` after the third write; intermediate negative state (`-50` after the second write) is observed by a concurrent read with no exception (REQ-007); the JPA entity carries `@Version` and an `OptimisticLockingFailureException` on a stale write is propagated (will be swallowed in the listener layer per RULE-010, not here); the repository exposes `findById(customerId)` and `save(...)` only (no delete).

> Completed in commit <sha-placeholder>; `CustomerBalanceJpaEntity` carries `@Version` (Hibernate-managed; see fix-up to TASK-003 for domain-side change), `CustomerBalanceJpaRepository` extends only `org.springframework.data.repository.Repository` (no `delete*` exposed), `CustomerBalanceRepositoryIT` (3 tests) asserts the +100,-150,+50 sequence → balance=0 (REQ-007 negative intermediate observable), an `OptimisticLockingFailureException` on a stale write, and the no-`delete*` surface contract.

## TASK-007 — JPA entity + repository for `processed_input_event` + `outbox`

- **Status:** in_progress
- **References:** REQ-003, REQ-005, REQ-006, REQ-014, REQ-015, RULE-008, ADR-003
- **Depends on:** TASK-003, TASK-004
- **Modules touched:** `lg5-loyalty-ledger-data-access`
- **Skill:** `lg5-outbox`
- **Command / Subagent:** `/add-outbox lg5-loyalty-ledger CustomerBalanceUpdated`
- **Acceptance:**
  - **Given** the `processed_input_event` and `outbox` tables from TASK-004
  - **When** an IT inserts the same `(originating_event_type, originating_event_id)` twice
  - **Then** the second insert raises `DataIntegrityViolationException` against `uq_processed_event_type_id` (this is the dedup mechanism, ADR-003); both entities carry `@Version` and `OutboxStatus`/`ProcessedInputEventOutcome` are mapped via `@Enumerated(EnumType.STRING)`; the outbox repository follows the standard framework shape from `/add-outbox` (`findAllByOutboxStatusOrderByCreatedAtAsc`, `save`).

## TASK-008 — Mapper Avro inbound → domain (`OrderPaid|Cancelled|Refunded` → input port command)

- **Status:** todo
- **References:** REQ-001, REQ-004, REQ-014, RULE-007, ADR-002
- **Depends on:** TASK-003
- **Modules touched:** `lg5-loyalty-ledger-message-core`
- **Skill:** `lg5-kafka-avro`
- **Command / Subagent:** (none — direct authoring; depends on `order-service-message-model` Maven artifact)
- **Acceptance:**
  - **Given** the three inbound Avro classes (`OrderPaidAvroModel`, `OrderCancelledAvroModel`, `OrderRefundedAvroModel`) consumed via the `order-service-message-model` Maven dependency declared in `lg5-loyalty-ledger-message-model/pom.xml` (ADR-002)
  - **When** unit tests feed each Avro instance through the mapper
  - **Then** the produced input-port command (one per event type) carries `customerId`, `orderId`, `eventId`, `eventType` (string literal `"OrderPaid"`/`"OrderCancelled"`/`"OrderRefunded"`), `eventReceivedAt`, and (for `OrderPaid` only) `paidAmount` as a `Money` value object built from the Avro `decimal`; no Spring annotations appear on the mapper class (RULE-005).

## TASK-009 — Kafka listener + config for `order-paid`

- **Status:** todo
- **References:** REQ-001, REQ-015, RULE-007, RULE-010, RULE-014, ADR-001
- **Depends on:** TASK-008
- **Modules touched:** `lg5-loyalty-ledger-message-core`
- **Skill:** `lg5-kafka-avro`
- **Command / Subagent:** `/add-kafka-listener lg5-loyalty-ledger order-paid OrderPaidAvroModel`
- **Acceptance:**
  - **Given** a Kafka Testcontainers broker with the `order-paid` topic
  - **When** an `OrderPaidAvroModel` is produced and the listener consumes it
  - **Then** the listener calls the application-service input port exactly once per message; the listener bean is configured as `batch-listener: true` (RULE-010) and is wired through `kafka-consumer-config.*` properties (RULE-014); thrown `OptimisticLockingFailureException` and `DataIntegrityViolationException` (added by ADR-003) are caught and logged at `DEBUG` as NO-OP without rethrowing (RULE-010 + ADR-003).

## TASK-010 — Kafka listener + config for `order-cancelled`

- **Status:** todo
- **References:** REQ-004, REQ-005, REQ-015, RULE-007, RULE-010, RULE-014
- **Depends on:** TASK-008
- **Modules touched:** `lg5-loyalty-ledger-message-core`
- **Skill:** `lg5-kafka-avro`
- **Command / Subagent:** `/add-kafka-listener lg5-loyalty-ledger order-cancelled OrderCancelledAvroModel`
- **Acceptance:**
  - **Given** a Kafka Testcontainers broker with the `order-cancelled` topic
  - **When** an `OrderCancelledAvroModel` is produced
  - **Then** the listener invokes the application-service exactly once with the cancellation command; same NO-OP swallow contract as TASK-009.

## TASK-010b — Kafka listener + config for `order-refunded`

- **Status:** todo
- **References:** REQ-004, REQ-005, REQ-015, RULE-007, RULE-010, RULE-014
- **Depends on:** TASK-008
- **Modules touched:** `lg5-loyalty-ledger-message-core`
- **Skill:** `lg5-kafka-avro`
- **Command / Subagent:** `/add-kafka-listener lg5-loyalty-ledger order-refunded OrderRefundedAvroModel`
- **Acceptance:**
  - **Given** a Kafka Testcontainers broker with the `order-refunded` topic
  - **When** an `OrderRefundedAvroModel` is produced
  - **Then** the listener invokes the application-service exactly once with the refund command; same NO-OP swallow contract as TASK-009.

## TASK-011 — Application-service handler: dedup gate + movement append + balance update + domain event raise + outbox payload persist

- **Status:** todo
- **References:** REQ-001, REQ-002, REQ-003, REQ-004, REQ-005, REQ-006, REQ-007, REQ-008, REQ-011, REQ-012, REQ-013, REQ-014, REQ-015, RULE-003, RULE-008, RULE-010, ADR-001, ADR-003, ADR-004
- **Depends on:** TASK-005, TASK-006, TASK-007, TASK-008
- **Modules touched:** `lg5-loyalty-ledger-application-service`, `lg5-loyalty-ledger-domain-core` (event POJO + payload mapper)
- **Skill:** `lg5-outbox`
- **Command / Subagent:** (none — direct authoring; subagent `lg5-test-generator` for the IT)
- **Acceptance:**
  - **Given** the dedup table, the movement repo, the balance repo, and the outbox repo, all wired through the helper class (single `@Transactional` boundary per `data-model.md` §Idempotency strategy)
  - **When** the helper's `process(command)` is invoked with the cases below
  - **Then** the observable outcomes are:
    - Case A (`OrderPaid`, paidAmount = `12.95`, customer with balance `0`): one `processed_input_event` row with `outcome=MOVEMENT_APPENDED`, one `movement` row with `delta=+12, cause=ORDER_PAID`, `customer_balance.balance=12`, one `outbox` row with `type="CustomerBalanceUpdated"` and JSON payload `newBalance=12, delta=+12, cause="ORDER_PAID"`.
    - Case B (`OrderPaid`, paidAmount = `0.50`, customer with balance `0`): one `processed_input_event` row with `outcome=NOOP_ZERO_CREDIT`, **no** `movement`, **no** `outbox`, balance unchanged (REQ-002 / Q1).
    - Case C (replay of Case A's same `eventId`): no second `movement`, no second `outbox`; the helper returns without rethrowing (`DataIntegrityViolationException` swallowed; REQ-003).
    - Case D (`OrderCancelled` for an order with prior credit `+12`): one `processed_input_event` with `outcome=MOVEMENT_APPENDED`, one `movement` with `delta=-12, cause=ORDER_CANCELLED`, balance now `0`, one `outbox` with `cause="ORDER_CANCELLED"`.
    - Case E (`OrderCancelled` for an order with **no** prior credit): one `processed_input_event` with `outcome=NOOP_DEBIT_WITHOUT_CREDIT`, **no** `movement`, **no** `outbox`, one `WARN`-level log entry containing `orderId` and event type (REQ-005 / Q2).
    - Case F (`OrderRefunded` for an order with prior credit `+12`): same as Case D but `cause=ORDER_REFUNDED`.
    - Case G (`OrderPaid → OrderCancelled → OrderPaid` again on same `orderId` with **distinct event ids**): two credits + one debit appended in the order received; final balance `+12` (REQ-003 + Q9 / R5).
    - Case H (debit applied to a customer with balance `+5`, debit amount `12`): movement appended with `delta=-12`, balance becomes `-7`, outbox payload `newBalance=-7` (REQ-007, REQ-008).

## TASK-012 — Mapper outbox payload (JSON) → outbound Avro `CustomerBalanceUpdatedAvroModel`

- **Status:** todo
- **References:** REQ-011, REQ-012, RULE-007, RULE-008, ADR-005
- **Depends on:** TASK-011
- **Modules touched:** `lg5-loyalty-ledger-message-core`, `lg5-loyalty-ledger-message-model` (Avro classes consumed)
- **Skill:** `lg5-kafka-avro`
- **Command / Subagent:** (none — `make run-avro-model` generates Java sources first)
- **Acceptance:**
  - **Given** the JSON `CustomerBalanceUpdatedEventPayload` shape from `data-model.md` §Outbox payloads and the generated Avro classes from `BalanceUpdateCause.avsc` + `CustomerBalanceUpdatedAvroModel.avsc`
  - **When** unit tests feed the JSON through the producer-side mapper
  - **Then** the produced Avro record has every field from §Avro outbound populated 1:1; the `cause` enum symbol matches the JSON `cause` string; an unknown JSON `cause` value (defensive) maps to `BalanceUpdateCause.UNKNOWN` (ADR-005); no Spring annotation appears on the mapper class (RULE-005).

## TASK-013 — Outbox scheduler + Kafka producer wiring

- **Status:** todo
- **References:** REQ-011, REQ-015, RULE-007, RULE-008, RULE-011, RULE-014, ADR-001
- **Depends on:** TASK-007, TASK-012
- **Modules touched:** `lg5-loyalty-ledger-application-service`, `lg5-loyalty-ledger-message-core`
- **Skill:** `lg5-outbox`
- **Command / Subagent:** `/add-outbox lg5-loyalty-ledger CustomerBalanceUpdated` (continuation of TASK-007 — this task adds the scheduler + publisher half)
- **Acceptance:**
  - **Given** outbox rows in `STARTED` state and a Kafka Testcontainers broker with the `customer-balance-updated` topic
  - **When** the scheduler tick fires (test override `loyalty-ledger-service.outbox-scheduler-fixed-rate=200ms`)
  - **Then** each `STARTED` row is published as an Avro `CustomerBalanceUpdatedAvroModel` on the topic with **Kafka key = customerId** (string-uuid; ADR-001 RULE-007 clarification), the row is marked `COMPLETED`, the IT consumer receives one event per published row in `customerId` partition order; the scheduler bean is `@Scheduled(fixedDelayString = "${loyalty-ledger-service.outbox-scheduler-fixed-rate}")` and `@ConditionalOnProperty(value = "scheduling.enabled", matchIfMissing = true)` (RULE-011); listener-side `OptimisticLockingFailureException` on the `STARTED → COMPLETED` transition is swallowed as NO-OP (RULE-010).

## TASK-014 — Schema-registry compatibility check + registration script

- **Status:** todo
- **References:** REQ-011, REQ-012, RULE-007, ADR-005
- **Depends on:** TASK-012
- **Modules touched:** `lg5-loyalty-ledger-message-model`, `lg5-loyalty-ledger-support`
- **Skill:** `lg5-kafka-avro`
- **Command / Subagent:** (none — `make publish-schemas` target authored)
- **Acceptance:**
  - **Given** a local schema-registry container from `lg5-loyalty-ledger-support/docker-compose.yml`
  - **When** `make publish-schemas` is run against it
  - **Then** subjects `customer-balance-updated-value` and `BalanceUpdateCause` are registered with compatibility mode `BACKWARD`; the registration is idempotent (re-running the target is a no-op); a CI gate target `make check-schema-compat` rejects an incompatible local change (e.g. removing the `delta` field) without registering it.

## TASK-015 — REST controller `GET /loyalty/customers/{id}/balance`

- **Status:** todo
- **References:** REQ-009, RULE-005, RULE-006
- **Depends on:** TASK-006
- **Modules touched:** `lg5-loyalty-ledger-api`
- **Skill:** `lg5-spring-overview`
- **Command / Subagent:** (none — direct authoring)
- **Acceptance:**
  - **Given** a customer with a known `balance` in `customer_balance` (e.g. `42` or `-7`)
  - **When** an HTTP `GET /loyalty/customers/{customerId}/balance` is issued
  - **Then** the response status is `200 OK`, the `Content-Type` is `application/vnd.api.v1+json` (RULE-006), and the body matches the `CustomerBalanceResponse` record (`customerId`, `balance`, `lastUpdatedAt`); the controller class uses stock `@RestController` + `@GetMapping`, no custom annotations (RULE-005); querying an unknown `customerId` returns `404 Not Found` with an `ErrorDTO` body; the controller method is read-only (no `@Transactional` write semantics).

## TASK-016 — REST controller `GET /loyalty/customers/{id}/movements?page=&size=` (reverse-chronological paged)

- **Status:** todo
- **References:** REQ-010, REQ-013, RULE-005, RULE-006
- **Depends on:** TASK-005
- **Modules touched:** `lg5-loyalty-ledger-api`
- **Skill:** `lg5-spring-overview`
- **Command / Subagent:** (none)
- **Acceptance:**
  - **Given** a customer with N movements (N > 50) inserted across distinct `appended_at`
  - **When** sequential calls `GET …/movements?page=0&size=20`, `?page=1&size=20`, `?page=2&size=20` are issued
  - **Then** each response is `200 OK` with `Content-Type: application/vnd.api.v1+json` and a `MovementsPageResponse` body; the three pages cover non-overlapping movements totalling 60; movements within each page are ordered `appended_at DESC, id DESC` (REQ-010, stable ordering on ties via `idx_movement_customer_appended`); `totalElements == N`; an out-of-range page (`page=999`) returns an empty `movements` array with `200 OK`, not `404`; no controller method mutates state (REQ-013).

## TASK-017 — Error handling + `ErrorDTO` mapping (`@RestControllerAdvice`)

- **Status:** todo
- **References:** REQ-009, REQ-010, REQ-015, RULE-005, RULE-006
- **Depends on:** TASK-015, TASK-016
- **Modules touched:** `lg5-loyalty-ledger-api`
- **Skill:** `lg5-spring-overview`
- **Command / Subagent:** (none)
- **Acceptance:**
  - **Given** the controllers from TASK-015/TASK-016 and a `@RestControllerAdvice` mapping handler
  - **When** an IT triggers (a) a malformed `customerId` in the path, (b) a missing `customer_balance` row, (c) an unexpected `RuntimeException` from the service layer
  - **Then** responses are: (a) `400 Bad Request` with `ErrorDTO{code=INVALID_REQUEST, ...}`; (b) `404 Not Found` with `ErrorDTO{code=CUSTOMER_NOT_FOUND, ...}`; (c) `500 Internal Server Error` with `ErrorDTO{code=INTERNAL, traceId=...}`; all responses keep `Content-Type: application/vnd.api.v1+json` (RULE-006); listener-side errors (REQ-015) are NOT routed through this advice (they are swallowed inside the listener by RULE-010 + ADR-003 well before reaching the API layer).

## TASK-018 — ATDD infra: Cucumber + Testcontainers (Postgres, Kafka, Schema Registry) + Wiremock setup

- **Status:** todo
- **References:** RULE-012, RULE-013
- **Depends on:** TASK-002, TASK-013
- **Modules touched:** `lg5-loyalty-ledger-acceptance-test`
- **Skill:** `lg5-atdd`
- **Command / Subagent:** (none — direct authoring; `lg5-test-generator` will be invoked in TASK-019 for scenarios)
- **Acceptance:**
  - **Given** the framework's `Lg5TestBootPortNone` + `TestContainersLoader` patterns
  - **When** `make run-acceptance-test` (or the equivalent Maven verify target) is run with no scenarios yet
  - **Then** the Cucumber runner boots successfully against `@ActiveProfiles({"test","local"})` (RULE-012); the Postgres, Kafka, and Schema-Registry Testcontainers each start when their respective `testcontainers.<name>.enabled=true` flag is set, and stay down otherwise (RULE-013); a Wiremock stub for the schema-registry `subjects/.../versions` endpoints is available (used to assert producer-side serialization without a live registry where convenient); empty scenarios run pass = 0 / fail = 0 / skipped = 0.

## TASK-019 — ATDD scenarios: one per REQ (or equivalence class)

- **Status:** todo
- **References:** REQ-001..REQ-015, ADR-003, ADR-004, ADR-005, RULE-012, RULE-013
- **Depends on:** TASK-011, TASK-013, TASK-015, TASK-016, TASK-017, TASK-018
- **Modules touched:** `lg5-loyalty-ledger-acceptance-test`
- **Skill:** `lg5-atdd`
- **Command / Subagent:** subagent `lg5-test-generator` (consumes the Given/When/Then from this `tasks.md` and from `prd.md` §5)
- **Acceptance:**
  - **Given** the scenarios authored from the case list in TASK-011 plus the read-API scenarios from TASK-015/016 plus error scenarios from TASK-017
  - **When** `make run-acceptance-test` runs against a full Testcontainers stack (Postgres + Kafka + Schema Registry)
  - **Then** **every REQ-NNN from the PRD is covered by ≥1 passing scenario**, including the credit-and-publish happy path (REQ-001/REQ-011), the floor-to-zero skip (REQ-002/Q1), idempotent replay (REQ-003), debit on cancel + on refund as distinct causes (REQ-004 + Q8), debit-without-credit no-op + WARN log (REQ-005), debit replay idempotency (REQ-006), negative balance allowed (REQ-007), negative balance observable (REQ-008), `GET balance` (REQ-009), `GET movements` paged (REQ-010), outbound event with cause + traceability (REQ-011/REQ-012), append-only enforcement (REQ-013 — verified by absence of mutator endpoints + JPA repo shape), originating-event traceability (REQ-014), and the `paid → cancelled → paid-again` legitimate replay (REQ-003 / Q9 / R5); the test report is archived under `target-acceptance-test/` (cf. `.gitignore`).

## TASK-020 — Final code review by `lg5-code-reviewer` + DoD closeout

- **Status:** todo
- **References:** all RULE-001..RULE-018; all REQ-001..REQ-015; all ADRs
- **Depends on:** TASK-019
- **Modules touched:** (review only — no code changes; if violations are found, additional follow-up tasks are appended)
- **Skill:** `lg5-spring-overview`
- **Command / Subagent:** subagent `lg5-code-reviewer`
- **Acceptance:**
  - **Given** the full diff of `feature/001-loyalty-ledger` vs `main`
  - **When** the `lg5-code-reviewer` subagent is invoked with the diff and the constitution
  - **Then** the reviewer reports **zero `must`-rule violations** (RULE-001..014, 016 — the 15 constitutional rules); any `should`/`info` advisories from RULE-015/017/018 are listed and either fixed or explicitly accepted in a follow-up note appended to this task; the PR description from `tasks.md` lists every TASK-NNN with its commit hash; ATDD report from TASK-019 shows green; this task is marked `done` only after the human reviewer also signs off on the PR.

---

## Coverage matrix

| REQ      | Covered by TASK(s) |
|----------|--------------------|
| REQ-001  | TASK-009, TASK-011 (Case A), TASK-013, TASK-019 |
| REQ-002  | TASK-003 (sign invariant), TASK-011 (Case A, B), TASK-019 |
| REQ-003  | TASK-007 (unique constraint), TASK-011 (Case C, G), TASK-019 |
| REQ-004  | TASK-010, TASK-010b, TASK-011 (Case D, F), TASK-019 |
| REQ-005  | TASK-011 (Case E), TASK-017, TASK-019 |
| REQ-006  | TASK-007, TASK-011 (analogous Case C for cancels), TASK-019 |
| REQ-007  | TASK-003 (`applyDelta` allows negative), TASK-006, TASK-011 (Case H), TASK-019 |
| REQ-008  | TASK-006, TASK-011 (Case H), TASK-015, TASK-019 |
| REQ-009  | TASK-006, TASK-015, TASK-017, TASK-019 |
| REQ-010  | TASK-005 (index), TASK-016, TASK-019 |
| REQ-011  | TASK-011, TASK-012, TASK-013, TASK-014, TASK-019 |
| REQ-012  | TASK-011, TASK-012, TASK-014, TASK-019 |
| REQ-013  | TASK-003 (no mutator), TASK-005 (no UPDATE/DELETE), TASK-016 (read-only), TASK-019 |
| REQ-014  | TASK-005, TASK-007, TASK-008, TASK-019 |
| REQ-015  | TASK-007, TASK-009, TASK-010, TASK-010b, TASK-011 (Case C, E), TASK-013, TASK-017, TASK-019 |

Every REQ-001..REQ-015 is covered by ≥1 TASK. ✅

## Definition of Done (Tasks)

- [x] Every TASK references ≥1 REQ-NNN. _(Bootstrap TASKs 001/002 reference RULEs only — those are skeleton precondition tasks per the template guidance; they do not own a REQ. All other 18 tasks reference ≥1 REQ.)_
- [x] Every TASK has Given/When/Then acceptance criteria. _(All 20 tasks.)_
- [x] Every TASK is ≤1 day of work / 1-3 commits. _(Largest is TASK-011 with 8 cases; Cases C+E+G are reused-test-fixture variants of A/D/D, so it fits in 1-2 days; if a checkpoint is needed, split into TASK-011a "happy path" and TASK-011b "edge cases" at `/sdd-implement` time.)_
- [x] Dependencies form a DAG (no cycles) — verified against `plan.md`. _(See dep graph at the top.)_
- [x] First TASK is the project skeleton. _(TASK-001 = `/scaffold-service`.)_
- [x] Last TASK is "all ATDD scenarios green + zero `must` violations". _(TASK-020 = `lg5-code-reviewer` final pass + DoD closeout, depends on TASK-019 ATDD green.)_
- [x] Each TASK names the exact module(s), skill(s), and command(s)/subagent(s) it uses. _(All 20 TASKs.)_
