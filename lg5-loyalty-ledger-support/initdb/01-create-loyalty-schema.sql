-- Create the 'loyalty' schema inside the loyalty_ledger database.
-- Tables and indexes are created by Flyway/Liquibase migrations from
-- lg5-loyalty-ledger-data-access (TASK-004).
CREATE SCHEMA IF NOT EXISTS loyalty AUTHORIZATION sa;
